[package]
name = "bombo_core"
version = "0.3.0"
edition = "2024"

[dependencies]
wide = "1.5"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
